syntax = "proto3";

message MathRequest {
  double x = 1;
  int64 y = 2;
}

message MathResponse {
  repeated double answer = 1;
}

service MathService {
  rpc count(MathRequest) returns (MathResponse);
}